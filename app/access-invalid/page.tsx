export const metadata = { title: "Access link unavailable", robots: { index: false, follow: false } };

export default function AccessInvalidPage() {
  return <main className="access-landing"><section><span className="brand-mark">SD</span><p className="eyebrow"><span /> Link access</p><h1>This access link is no longer available.</h1><p>It may have already been used, expired, or been revoked. Ask a current officer to create a new link.</p></section></main>;
}
